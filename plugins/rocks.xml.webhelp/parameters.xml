<?xml version="1.0" encoding="UTF-8"?>
<params xmlns:if="ant:if">
    <param name="using-input" expression="${args.input}"/>
    <param name="includes-pdf" expression="${includes-pdf}" if:set="includes-pdf"/>
    <param name="table-numbering" expression="${table-numbering}" if:set="table-numbering"/>
    <param name="figure-numbering" expression="${figure-numbering}" if:set="figure-numbering"/>
    <param name="save-to-google-drive" expression="${save-to-google-drive}" if:set="save-to-google-drive"/>
    <param name="organization-name" expression="${organization-name}" if:set="organization-name"/>
    <param name="two-col-fig-callouts" expression="${two-col-fig-callouts}" if:set="two-col-fig-callouts"/>
    <param name="name-of-map" expression="${name-of-map}"/>
    <param name="header-bg-color" expression="${header-bg-color}" if:set="header-bg-color"/>
    <param name="accent-bg-color" expression="${accent-bg-color}" if:set="accent-bg-color"/>
    <param name="accent-color" expression="${accent-color}" if:set="accent-color"/>
    <param name="footer-bg-color" expression="${footer-bg-color}" if:set="footer-bg-color"/>
    <param name="rocks-xml-webhelp-dir" expression="${dita.plugin.rocks.xml.webhelp.dir}" if:set="dita.plugin.rocks.xml.webhelp.dir"/>
</params>